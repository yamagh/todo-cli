{
  "name": "@yamagh/todo-cli",
  "version": "1.0.1",
  "description": "Task management CLI for AI agents",
  "main": "bin/todo.js",
  "bin": {
    "todo": "bin/todo.js"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "commander": "^12.0.0",
    "write-file-atomic": "^5.0.1"
  }
}
